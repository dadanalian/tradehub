services:
  - type: web
    name: tradehub
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -c "from app import app; app.run(host='0.0.0.0', port=${PORT:-5000}, debug=False, use_reloader=False)"
    plan: free
